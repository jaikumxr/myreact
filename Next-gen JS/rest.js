//similar to JAVA
//

const filter = (...args) =>{
    //in built filter method available on arrays
    return args.filter(el => el===1); //returns elements if they are equal to 1
}

console.log(filter(1,2,3));

